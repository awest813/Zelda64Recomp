diff --git a/librecomp/include/librecomp/addresses.hpp b/librecomp/include/librecomp/addresses.hpp
index b1b50cb..a5ffa2c 100644
--- a/librecomp/include/librecomp/addresses.hpp
+++ b/librecomp/include/librecomp/addresses.hpp
@@ -6,10 +6,19 @@
 #include "recomp.h"
 
 namespace recomp {
+#ifdef DREAMCAST
+    // Dreamcast: fit RDRAM emulation into main RAM (8 MB game region + extended
+    // area + recomp heap). No sparse multi-GB mmap reservation.
+    constexpr size_t mem_size = 14ULL * 1024ULL * 1024ULL;
+    constexpr size_t allocation_size = mem_size;
+    // Heap starts below mod_rdram_start because mods are disabled on Dreamcast.
+    constexpr uint32_t dc_heap_start = 0x80900000;
+#else
     // 512GB (kseg0 size)
     constexpr size_t mem_size = 512ULL * 1024ULL * 1024ULL;
     // 4GB (the full address space)
     constexpr size_t allocation_size = 4096ULL * 1024ULL * 1024ULL;
+#endif
     // We need a place in rdram to hold the PI handles, so pick an address in extended rdram
     constexpr int32_t cart_handle = 0x80800000;
     constexpr int32_t drive_handle = (int32_t)(cart_handle + sizeof(OSPiHandle));
diff --git a/librecomp/include/librecomp/game.hpp b/librecomp/include/librecomp/game.hpp
index 56480d6..b12e8f7 100644
--- a/librecomp/include/librecomp/game.hpp
+++ b/librecomp/include/librecomp/game.hpp
@@ -77,6 +77,10 @@ namespace recomp {
     bool is_rom_valid(std::u8string& game_id);
     bool is_rom_loaded();
     void set_rom_contents(std::vector<uint8_t>&& new_rom);
+#ifdef DREAMCAST
+    // Stream PI reads from a ROM file on GD-ROM instead of holding the image in RAM.
+    void set_rom_stream(const std::filesystem::path& path, size_t size);
+#endif
     std::span<const uint8_t> get_rom();
     void do_rom_read(uint8_t* rdram, gpr ram_address, uint32_t physical_addr, size_t num_bytes);
     void do_rom_pio(uint8_t* rdram, gpr ram_address, uint32_t physical_addr);
diff --git a/librecomp/src/pi.cpp b/librecomp/src/pi.cpp
index d7ebd53..6dd3cbe 100644
--- a/librecomp/src/pi.cpp
+++ b/librecomp/src/pi.cpp
@@ -4,6 +4,7 @@
 #include <cstring>
 #include <string>
 #include <mutex>
+#include <cstdio>
 #include "recomp.h"
 #include "librecomp/addresses.hpp"
 #include "librecomp/game.hpp"
@@ -13,14 +14,53 @@
 
 static std::vector<uint8_t> rom;
 
+#ifdef DREAMCAST
+static std::filesystem::path rom_stream_path;
+static size_t rom_stream_size = 0;
+static std::mutex rom_stream_mutex;
+
+static bool rom_stream_read(uint32_t offset, void* dest, size_t count) {
+    if (offset + count > rom_stream_size) {
+        return false;
+    }
+    std::lock_guard lock{rom_stream_mutex};
+    FILE* f = fopen(rom_stream_path.c_str(), "rb");
+    if (f == nullptr) {
+        return false;
+    }
+    bool ok = false;
+    if (fseek(f, static_cast<long>(offset), SEEK_SET) == 0) {
+        ok = fread(dest, 1, count, f) == count;
+    }
+    fclose(f);
+    return ok;
+}
+#endif
+
 bool recomp::is_rom_loaded() {
+#ifdef DREAMCAST
+    return !rom.empty() || rom_stream_size != 0;
+#else
     return !rom.empty();
+#endif
 }
 
 void recomp::set_rom_contents(std::vector<uint8_t>&& new_rom) {
     rom = std::move(new_rom);
+#ifdef DREAMCAST
+    rom_stream_path.clear();
+    rom_stream_size = 0;
+#endif
 }
 
+#ifdef DREAMCAST
+void recomp::set_rom_stream(const std::filesystem::path& path, size_t size) {
+    rom.clear();
+    rom_stream_path = path;
+    rom_stream_size = size;
+}
+#endif
+
 std::span<const uint8_t> recomp::get_rom() {
     return rom;
 }
@@ -67,7 +107,22 @@ void recomp::do_rom_read(uint8_t* rdram, gpr ram_address, uint32_t physical_addr
     // TODO handle misaligned DMA
     assert((physical_addr & 0x1) == 0 && "Only PI DMA from aligned ROM addresses is currently supported");
     assert((ram_address & 0x7) == 0 && "Only PI DMA to aligned RDRAM addresses is currently supported");
-    uint8_t* rom_addr = rom.data() + physical_addr - recomp::rom_base;
+    const uint32_t rom_offset = physical_addr - recomp::rom_base;
+#ifdef DREAMCAST
+    if (rom_stream_size != 0) {
+        std::vector<uint8_t> chunk(num_bytes);
+        if (!rom_stream_read(rom_offset, chunk.data(), num_bytes)) {
+            fprintf(stderr, "[pi] ROM stream read failed at 0x%08X (%zu bytes)\n", rom_offset,
+                    num_bytes);
+            return;
+        }
+        for (size_t i = 0; i < num_bytes; i++) {
+            MEM_B(i, ram_address) = chunk[i];
+        }
+        return;
+    }
+#endif
+    uint8_t* rom_addr = rom.data() + rom_offset;
     for (size_t i = 0; i < num_bytes; i++) {
         MEM_B(i, ram_address) = *rom_addr;
         rom_addr++;
@@ -77,7 +132,22 @@ void recomp::do_rom_read(uint8_t* rdram, gpr ram_address, uint32_t physical_addr
 void recomp::do_rom_pio(uint8_t* rdram, gpr ram_address, uint32_t physical_addr) {
     assert((physical_addr & 0x3) == 0 && "PIO not 4-byte aligned in device, currently unsupported");
     assert((ram_address & 0x3) == 0 && "PIO not 4-byte aligned in RDRAM, currently unsupported");
-    uint8_t* rom_addr = rom.data() + physical_addr - recomp::rom_base;
+    const uint32_t rom_offset = physical_addr - recomp::rom_base;
+#ifdef DREAMCAST
+    if (rom_stream_size != 0) {
+        uint8_t word[4];
+        if (!rom_stream_read(rom_offset, word, sizeof(word))) {
+            fprintf(stderr, "[pi] ROM stream PIO read failed at 0x%08X\n", rom_offset);
+            return;
+        }
+        MEM_B(0, ram_address) = word[0];
+        MEM_B(1, ram_address) = word[1];
+        MEM_B(2, ram_address) = word[2];
+        MEM_B(3, ram_address) = word[3];
+        return;
+    }
+#endif
+    uint8_t* rom_addr = rom.data() + rom_offset;
     MEM_B(0, ram_address) = *rom_addr++;
     MEM_B(1, ram_address) = *rom_addr++;
     MEM_B(2, ram_address) = *rom_addr++;
diff --git a/librecomp/src/recomp.cpp b/librecomp/src/recomp.cpp
index 8a03dbd..fd7044a 100644
--- a/librecomp/src/recomp.cpp
+++ b/librecomp/src/recomp.cpp
@@ -642,8 +642,10 @@ bool wait_for_game_started(uint8_t* rdram, recomp_context* context) {
         // TODO refactor this to allow a project to specify what entrypoint function to run for a give game.
         case GameStatus::Running:
             {
-                if (!recomp::load_stored_rom(current_game.value())) {
-                    ultramodern::error_handling::message_box("Error opening stored ROM! Please restart this program.");
+                if (!recomp::is_rom_loaded()) {
+                    if (!recomp::load_stored_rom(current_game.value())) {
+                        ultramodern::error_handling::message_box("Error opening stored ROM! Please restart this program.");
+                    }
                 }
 
                 auto find_it = game_roms.find(current_game.value());
@@ -678,7 +680,11 @@ bool wait_for_game_started(uint8_t* rdram, recomp_context* context) {
                     }
                 }
 
+#ifdef DREAMCAST
+                recomp::init_heap(rdram, recomp::dc_heap_start + mod_ram_used);
+#else
                 recomp::init_heap(rdram, recomp::mod_rdram_start + mod_ram_used);
+#endif
 
                 save_type = game_entry.save_type;
                 ultramodern::init_saving(rdram);
@@ -781,6 +787,10 @@ void recomp::start(
             VirtualFree(rdram, 0, MEM_RELEASE);
         }
     }
+#elif defined(DREAMCAST)
+    // KOS/newlib has no multi-GB anonymous mmap; allocate a fixed RDRAM buffer.
+    rdram = static_cast<uint8_t*>(std::calloc(1, allocation_size));
+    alloc_failed = (rdram == nullptr);
 #else
     rdram = (uint8_t*)mmap(NULL, allocation_size, PROT_NONE, MAP_ANON | MAP_PRIVATE, -1, 0);
     alloc_failed = rdram == reinterpret_cast<uint8_t*>(MAP_FAILED);
@@ -834,6 +844,9 @@ void recomp::start(
 #ifdef _WIN32
     // VirtualFree returns zero on failure.
     free_failed = (VirtualFree(rdram, 0, MEM_RELEASE) == 0);
+#elif defined(DREAMCAST)
+    std::free(rdram);
+    free_failed = false;
 #else
     // munmap returns -1 on failure.
     free_failed = (munmap(rdram, allocation_size) == -1);
